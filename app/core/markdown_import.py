"""Markdown plan import stubs."""

from __future__ import annotations

from pathlib import Path


def parse_markdown_plan(_: Path) -> dict[str, object]:
    """Parse a markdown plan file into structured data (placeholder)."""
    return {"days": []}
